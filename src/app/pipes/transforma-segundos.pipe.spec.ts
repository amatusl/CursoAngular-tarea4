import { TransformaSegundosPipe } from './transforma-segundos.pipe';

describe('TransformaSegundosPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformaSegundosPipe();
    expect(pipe).toBeTruthy();
  });
});
